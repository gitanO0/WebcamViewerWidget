<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="de.appphil.webcamviewerwidget.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/linklist"
        android:id="@+id/menu_btn_linklist"
        android:layout_above="@+id/menu_btn_settings"
        android:layout_alignStart="@+id/menu_btn_settings"
        android:layout_alignEnd="@+id/menu_btn_settings" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/settings"
        android:id="@+id/menu_btn_settings"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/menu_btn_info"
        android:layout_alignEnd="@+id/menu_btn_info" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/information"
        android:id="@+id/menu_btn_info"
        android:layout_below="@+id/menu_btn_settings"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
